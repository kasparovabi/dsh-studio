name: DshStudio
options:
  bundleIdPrefix: com.kasparov
  deploymentTarget:
    macOS: "14.0"
    iOS: "17.0"
targets:
  DshStudio:
    type: application
    platform: macOS
    sources:
      - path: Sources
        excludes:
          - iOS
      - path: Resources/AppIcon.icns
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: DSH Studio
        CFBundleIconFile: AppIcon
        # A tailnet address sits in 100.64.0.0/10, which local networking does
        # not cover on macOS, so plain http to another machine is refused before
        # the request leaves the app. Which addresses may be reached is decided
        # in code instead, by AppModel.isPrivateHost.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        LSApplicationCategoryType: public.app-category.developer-tools
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.kasparov.dshstudio
        CODE_SIGN_IDENTITY: "-"
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        ENABLE_HARDENED_RUNTIME: YES
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
  DshStudioTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/DshStudioTests
    dependencies:
      - target: DshStudio
    settings:
      base:
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        GENERATE_INFOPLIST_FILE: YES

schemes:
  DshStudio:
    build:
      targets:
        DshStudio: all
        DshStudioTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - DshStudioTests
